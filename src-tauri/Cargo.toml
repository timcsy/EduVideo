[package]
name = "eduvideo-studio"
version = "0.1.0"
description = "Cross-platform teaching video recorder and editor"
authors = ["EduVideo"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
